{
  "configurations": [
    {
      "name": "windows-gcc-x64",
      "includePath": [
        "${workspaceFolder}/**"
      ],
      // Removed "compilerPath" because "gcc" could not be found; set this to the full path to your GCC installation if you have one, e.g. "C:/msys64/mingw64/bin/gcc.exe"
      "cStandard": "${default}",
      "cppStandard": "${default}",
      "intelliSenseMode": "windows-gcc-x64",
      "compilerArgs": [
        ""
      ]
    }
  ],
  "version": 4
}